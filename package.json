{
  "name": "apex-gym-workspace",
  "version": "1.0.0",
  "description": "Root configuration to run both frontend and backend concurrently",
  "scripts": {
    "start": "npx concurrently \"npm start --prefix aalq-gym-backend\" \"npm start --prefix aaql-gym-frontend\""
  }
}
